/**
* Validation code for field resume on resumes
* @param {import("gadget-server").ResumesResumeFieldValidationContext } context - All the useful bits for running this validation.
*/
export default async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.resume` to get the fields's data
  // add to the `errors` with `errors.add('resume', "did not pass the validation")`
};